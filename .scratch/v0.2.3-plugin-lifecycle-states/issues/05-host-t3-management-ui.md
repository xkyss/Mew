# 05: 宿主侧 T3 管理 UI

**What to build:** 宿主窗口/托盘长出 T3 独立插件的管理入口：列出 exe 插件行（状态词与动作由 01 推导），提供启用/禁用开关与重启动作（接线到 04 的进程托管）；已崩溃行动作变「重启」；不依赖扩展主机活着即可管理 T3。宿主自身开关直接落盘 `plugins.json`（宿主本就是唯一写者，与 03 的写权力归一自洽）。

**Blocked by:** 01, 04

**Status:** resolved

- [x] 宿主侧列出 exe 插件行：已启用 / 已禁用 / 已崩溃，清单错误标红
- [x] 每行一个动作：健康行 = 启用/禁用开关；已崩溃行动作变「重启」
- [x] 禁用 T3 → 立即杀进程且来源消失；重启 → 宿主重新拉起该进程
- [x] 扩展主机未运行时 T3 行仍可见可管
- [x] 无 T3 场景（如仅 DLL 插件）下列表正常为空或提示，不回归

## Comments

- 已完成。宿主占位窗新增「独立插件（T3）」区块：行列表自 `PluginLifecycleEngine.Snapshot` + 启用存储推导（`PluginRowState.Derive`，健康/清单错误/已崩溃同词），行内单动作按钮接线 `ApplyHostPluginAction`（禁用 → 落盘 + `Stop` 杀进程；启用 → 落盘 + `Start` 拉起；重启 → `Restart`）。
- 托盘右键新增「插件管理」项（`MenuPluginManager`）亮出宿主窗口 → 扩展主机未运行时 T3 依旧可达可管；宿主本就是唯一写者，开关直接 `Save()` 与 03 自洽。
- 引擎状态变化来自后台线程（进程退出/IPC 断连），经自窗口 `WM_APP+2` PostMessage 转到 UI 线程重建行，避免跨线程改 UI 树。
- 装配（窗口/托盘菜单/消息路由）需窗口，无头单测不可达，走代码走查验证（与托盘分发先例一致）；托盘分发表新增用例锁定（`TrayIconMenuTests` +1）。
- 相关测试：宿主测试工程全量通过（105 → 106 用例）。
