# 06: 版本提升与文档措辞扫尾

**What to build:** 生命周期功能全部落地后收尾：`AppVersion` 常量提升到 v0.2.3（运行时版本唯一来源单行改动）；`docs/run.md` 等运行文档中「卸载 ALC」式措辞统一改为「回收/释放」，与词汇表（插件启用/禁用、插件安装/卸载 = 存在性、卸载仅指 uninstall）及 ADR-000203 对齐。

**Blocked by:** 02, 03, 04, 05

**Status:** resolved

- [x] 运行时版本号提升为 v0.2.3（单行常量，先例 ba2885e / 6e02e1a）
- [x] 运行文档措辞与词汇表一致：卸载仅指 uninstall，运行态释放写作回收/释放
- [x] 设置→插件列表可见状态与 spec/ADR 一致（已启用/已禁用/清单错误/ID 重复/需 JIT/已崩溃）

## Comments

- 已完成。`MewHost.cs` `AppVersion` v0.2.2 → v0.2.3（单行常量改动）。
- `docs/run.md` 同步：头部版本与 ADR 引用 → v0.2.3；托盘右键项补「插件管理」与宿主窗口 T3 行说明；T2/T3 生命周期段按 ADR-000203 重写（卸载仅指 uninstall、T3 崩溃不落盘 + 防循环、T2 禁用 = 下次主界面启动不再装载），删除「ALC 卸载限制」旧措辞；`PluginDllLoader` 注释「卸载 ALC」改「释放 ALC」。
- 相关测试：宿主测试工程全量通过（106 用例），宿主工程构建通过。
