# 05: 宿主托盘常驻无主窗口

**What to build:** 宿主首启直接隐藏到托盘，不再展示主窗口；托盘左键呼出搜索浮层，右键菜单保留显示主窗口与打开/重启扩展主机，宿主窗口仅作备用入口。

**Blocked by:** 01 (宿主首启与手动拉起解绑，托盘分发缝在其上扩展)。

**Status:** resolved

- [x] 宿主首启直接隐藏，仅留托盘与呼出浮层常驻
- [x] 托盘左键呼出搜索浮层，不再显示主窗口
- [x] 右键菜单保留显示主窗口、打开/重启扩展主机与退出
- [x] 左键分发经纯逻辑缝覆盖单测（自定义动作与回退显示主窗口）

## Comments

- 已完成。首启 `Hide()`，左键经新增 `DispatchLeftClick` 纯缝呼出浮层（空动作回退显示主窗口）；`TrayIconMenuTests` 补 2 用例（8/8 通过）。
- 相关文档：`docs/run.md` 常驻语义与 `docs/adr/000202-01` 手动入口已同步。
