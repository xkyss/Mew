# 02: 退出不杀、崩溃只标记、关闭即隐藏

**What to build:** 宿主退出不再终止扩展主机，扩展主机退出后宿主仅做已崩溃标记、提示与日志而不自愈；关闭五区窗口只是隐藏秒显，真退出只走明确动作，任一端崩溃另一端仍可用。

**Blocked by:** 01 (宿主首启与手动拉起解绑，先定死拉起语义再断言退出与崩溃)。

**Status:** ready-for-agent

- [ ] 退出宿主后扩展主机进程保留
- [ ] 扩展主机退出或崩溃后宿主仅标记已崩溃或已退出并提示与记日志，不做定时自愈拉起
- [ ] 关闭五区窗口只是隐藏（下次秒显），真退出只走明确的终止动作且文案可区分
- [ ] 任一端崩溃后另一端常驻或五区能力仍可用，状态可从日志定位
- [ ] 行为测试覆盖退出不杀、崩溃仅标记不自愈、隐藏非退出
