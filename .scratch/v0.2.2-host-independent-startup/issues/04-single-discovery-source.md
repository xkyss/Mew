# 04: 发现归一与特殊容器身份

**What to build:** 目录扫描、清单校验、启用持久化只留宿主一份事实来源，扩展主机去掉自扫、只按接收到的待装名单加载；扩展主机不进插件列表、不提供禁用；禁用独立插件后其搜索源、设置节、热键从浮层消失。

**Blocked by:** 03 (插件分工与置灰，先定死两边各管什么再收敛名单来源)。

**Status:** resolved

- [x] 两端插件视图一致，来源只有宿主一份扫描与启用持久化，扩展主机不再自扫目录
- [x] 扩展主机不出现在插件列表中，不提供启用或禁用开关
- [x] 禁用某独立插件后其搜索源、设置节、热键立即从宿主侧消失
- [x] 独立插件崩溃仅该来源消失，其他来源与常驻能力不受影响
- [x] 行为测试覆盖名单一致性、特殊容器不进列表、禁用即消失、单源崩溃隔离

## Comments

- 已完成。新增发现快照（宿主扫描后写 `%APPDATA%/Mew/plugin-snapshot.json`，扩展主机优先按单加载，缺失或损坏回退本地扫描，保证双击独立可用）；宿主保留身份（`mew-host` / `mew-plugin-host`）在快照消费端与扩展主机列表双处过滤，冒名清单永不进列表。
- 名单一致性、回退、保留身份、按单加载隔离以 `PluginSnapshotTests`（4 用例）锁定；禁用即消失与单源崩溃隔离沿用 `PluginEnableStore` + `IpcServer.Unregister` 既有缝（`PluginDiscoveryTests` / `FaultIsolationTests` 覆盖）。
- 相关文档：`docs/run.md` 启用态与排障已同步。
