# 04: 发现归一与特殊容器身份

**What to build:** 目录扫描、清单校验、启用持久化只留宿主一份事实来源，扩展主机去掉自扫、只按接收到的待装名单加载；扩展主机不进插件列表、不提供禁用；禁用独立插件后其搜索源、设置节、热键从浮层消失。

**Blocked by:** 03 (插件分工与置灰，先定死两边各管什么再收敛名单来源)。

**Status:** ready-for-agent

- [ ] 两端插件视图一致，来源只有宿主一份扫描与启用持久化，扩展主机不再自扫目录
- [ ] 扩展主机不出现在插件列表中，不提供启用或禁用开关
- [ ] 禁用某独立插件后其搜索源、设置节、热键立即从宿主侧消失
- [ ] 独立插件崩溃仅该来源消失，其他来源与常驻能力不受影响
- [ ] 行为测试覆盖名单一致性、特殊容器不进列表、禁用即消失、单源崩溃隔离
