# 01: 宿主首启与手动拉起解绑

**What to build:** 宿主首启只见宿主窗口且不自动隐藏，显示主窗口与托盘左键不再顺手拉起五区；仅经宿主窗口按钮或托盘右键手动项才拉起或重启扩展主机。

**Blocked by:** None (can start immediately).

**Status:** resolved

- [x] 宿主首启停留在宿主窗口，写清托盘与呼出浮层常驻中、五区未启动，且不自动隐藏
- [x] 显示宿主主窗口与托盘左键只显示宿主，不触发拉起
- [x] 宿主窗口保留打开与重启扩展主机两个手动按钮，托盘右键新增打开与重启工作台捷径，点的那下才拉起
- [x] 宿主单跑时托盘、呼出浮层、全局热键全部可用
- [x] 行为测试覆盖首启不拉起、显示不拉起、手动触发拉起三条路径

## Comments

- 已完成。宿主装配接线（首启/显示不拉起）经代码走查验证（装配需窗口，无头单测不可达）；托盘分发表新增纯逻辑缝并以 `TrayIconMenuTests`（6 用例）锁定，全量相关测试通过。
- 托盘菜单新增“打开工作台/重启工作台”，未知 id 返回 false，空动作跳过不抛。
- 相关文档：`docs/run.md` 启动语义已同步。
