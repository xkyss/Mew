# 04: 无窗口 Application.Run 评估（可选,升级回报票）

**What to build:** 0.20 官方支持无窗口 `Application.Run`(纯托盘/热键常驻场景)。评估能否用官方路径替换 `MewHost` 的 0 透明度首启技巧(`Opacity = 0` → Loaded 内 `Hide()`)。行为契约不变:首启全程无可见帧、告警路径(热键被占/主界面缺失)亮窗提示、托盘左键浮层右键菜单、`ShowHostPluginManager` 亮窗。可行则替换并保持契约;不可行则维持现状并在本票记录原因。**运行验证依赖 Windows 环境**(与 03 同机执行)。

**Blocked by:** 02

**Status:** resolved

- [x] 0.20 无窗口 Run 的官方形态确认(文档/探针)
- [ ] 可行:替换宿主首启编排,行为契约逐项保持 —— 暂缓,见 Comments
- [x] 不可行/暂缓:维持现状,原因记录于 Comments
- [x] Windows 侧走查确认首启无可见帧 —— 并入 03 清单,替换落地后执行

## Comments


## Comments

- 评估结论(2026-09-05):**API 可行性初步成立,替换暂缓,维持 0 透明度技巧**。
- 证据:0.20.2 `Application` 静态重载含 `Run(Action startup)` / `Run(Action<T>)`——官方无窗口形态存在(反射探针核实)。
- 暂缓原因:替换对象是宿主首启编排(`Opacity=0` → Loaded 内 `Hide()`,ADR-000202/eac094b 的「零可见帧」契约),其正确性**无法无头验证**——Run(Action) 是否维持消息循环、托盘/浮层是否照常常驻、告警路径亮窗时序如何,只能真实运行核对。盲换违反本仓库「零回归 + 走查门槛」纪律。
- 后续路径:03 走查确认现有编排在新版下正常后,若需要本回报(消除 opacity hack),立 v0.3.1 单票:替换 + Windows 侧走查四项契约(首启无帧/告警亮窗/托盘常驻/浮层热键)。
